# test

Test repository for the SESHAT → IP Secure pipeline (Menezis project).

Every commit is automatically sealed via a `post-commit` hook
(see `.githooks/post-commit`) which anchors an eIDAS proof on Menezis.
